# Auto-generated by compose2nix.

{ pkgs, lib, config, ... }:

{
  # Runtime
  virtualisation.podman = {
    enable = true;
    autoPrune.enable = true;
    dockerCompat = true;
  };

  # Enable container name DNS for all Podman networks.
  networking.firewall.interfaces = let
    matchAll = if !config.networking.nftables.enable then "podman+" else "podman*";
  in {
    "${matchAll}".allowedUDPPorts = [ 53 ];
  };

  virtualisation.oci-containers.backend = "podman";

  # Containers
  virtualisation.oci-containers.containers."speedtest" = {
    image = "ghcr.io/librespeed/speedtest:latest";
    environment = {
      "MODE" = "standalone";
    };
    ports = [
      "8081:8080/tcp"
    ];
    log-driver = "journald";
    extraOptions = [
      "--network-alias=speedtest"
      "--network=librespeed_default"
    ];
  };
  systemd.services."podman-speedtest" = {
    serviceConfig = {
      Restart = lib.mkOverride 90 "always";
    };
    after = [
      "podman-network-librespeed_default.service"
    ];
    requires = [
      "podman-network-librespeed_default.service"
    ];
    partOf = [
      "podman-compose-librespeed-root.target"
    ];
    wantedBy = [
      "podman-compose-librespeed-root.target"
    ];
  };

  # Networks
  systemd.services."podman-network-librespeed_default" = {
    path = [ pkgs.podman ];
    serviceConfig = {
      Type = "oneshot";
      RemainAfterExit = true;
      ExecStop = "podman network rm -f librespeed_default";
    };
    script = ''
      podman network inspect librespeed_default || podman network create librespeed_default
    '';
    partOf = [ "podman-compose-librespeed-root.target" ];
    wantedBy = [ "podman-compose-librespeed-root.target" ];
  };

  # Root service
  # When started, this will automatically create all resources and start
  # the containers. When stopped, this will teardown all resources.
  systemd.targets."podman-compose-librespeed-root" = {
    unitConfig = {
      Description = "Root target generated by compose2nix.";
    };
    wantedBy = [ "multi-user.target" ];
  };
}
